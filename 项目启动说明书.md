# 项目启动说明书

这份说明书给第一次接手这个项目的同学用。

你不需要先完全看懂全部代码，只要按下面步骤做，就能把后端项目跑起来、跑测试、验证主链路。

---

## 1. 这个项目是什么

这是一个 `Spring Boot` 后端项目，主要负责：

1. 用户注册和登录
2. B 站账号绑定
3. 视频资产同步
4. 分析任务创建
5. 知识卡片创建
6. 问答接口返回

当前仓库里我们主要使用的是：

- `backend/`：后端主工程
- `backend/src/main/resources/application.yml`：后端配置
- `backend/.env.example`：环境变量示例
- `backend/README.md`：后端英文简版说明

---

## 2. 启动前你需要准备什么

先确认电脑上已经有下面这些东西：

1. `MySQL 8`
2. `Redis 7`
3. `Java 21`
4. 能正常使用 PowerShell

---

## 3. 先检查环境

在仓库根目录打开 PowerShell，先执行下面这些命令：

```powershell
java -version
Test-NetConnection -ComputerName 127.0.0.1 -Port 3306
Test-NetConnection -ComputerName 127.0.0.1 -Port 6379
```

你应该关注这几点：

1. `java -version` 最好是 `21`
2. MySQL 的 `3306` 端口能通
3. Redis 的 `6379` 端口能通

如果 `java -version` 还是 `1.8`，不要直接启动项目，先切到 `Java 21`。

---

## 4. 如果 Java 不是 21，怎么切换

本项目已经验证过的 JDK 21 路径是：

```powershell
C:\tmp\oracle-jdk-21
```

先执行：

```powershell
$env:JAVA_HOME="C:\tmp\oracle-jdk-21"
$env:Path="$env:JAVA_HOME\bin;$env:Path"
java -version
```

确认输出里已经是 `Java 21` 再继续。

---

## 5. 创建数据库

先保证 MySQL 已经启动，然后创建数据库：

```sql
CREATE DATABASE bili_agent;
```

数据库名必须叫：

```text
bili_agent
```

---

## 6. 准备本地环境变量文件

进入 `backend/` 目录，参考 `backend/.env.example` 创建一个你自己的本地文件：

```text
backend/.env.local
```

内容格式参考：

```env
APP_DB_URL=jdbc:mysql://127.0.0.1:3306/bili_agent?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
APP_DB_USERNAME=root
APP_DB_PASSWORD=你的MySQL密码
APP_JWT_SECRET=你自己本地用的JWT密钥
APP_AI_OPENAI_BASE_URL=https://api.openai.com/v1
APP_AI_OPENAI_API_KEY=
APP_AI_OPENAI_MODEL=deepseek-chat
```

注意：

1. `APP_DB_PASSWORD` 改成你自己的 MySQL 密码
2. `APP_JWT_SECRET` 自己随便设一个本地开发用字符串即可
3. `APP_AI_OPENAI_API_KEY` 现在可以先留空
4. `backend/.env.local` 不能提交到 git

---

## 7. 启动前先跑测试

先不要急着直接启动，先确认测试能过：

```powershell
backend\mvnw.cmd -f backend/pom.xml test
```

当前正常结果应该是：

```text
28 tests, 0 failures
```

如果这里不过，不要跳过，先修当前问题，再启动项目。

---

## 8. 启动项目的两种方式

### 方式 A：直接开发模式启动

先把环境变量注入当前 PowerShell：

```powershell
Get-Content backend\.env.local | ForEach-Object {
  $line = $_.Trim()
  if ($line -and -not $line.StartsWith("#")) {
    $parts = $line -split "=", 2
    [Environment]::SetEnvironmentVariable($parts[0], $parts[1], "Process")
  }
}
$env:APP_JWT_SECRET="bili-agent-demo-secret-key-bili-agent-demo"
```

然后启动：

```powershell
backend\mvnw.cmd -f backend/pom.xml spring-boot:run
```

---

### 方式 B：先打包，再用 jar 启动

如果你的本地 `spring-boot:run` 启动链路不稳定，推荐这个方式。

先打包：

```powershell
backend\mvnw.cmd -f backend/pom.xml -DskipTests package
```

然后把环境变量注入当前会话，再启动：

```powershell
Get-Content backend\.env.local | ForEach-Object {
  $line = $_.Trim()
  if ($line -and -not $line.StartsWith("#")) {
    $parts = $line -split "=", 2
    [Environment]::SetEnvironmentVariable($parts[0], $parts[1], "Process")
  }
}
$env:APP_JWT_SECRET="bili-agent-demo-secret-key-bili-agent-demo"
C:\tmp\oracle-jdk-21\bin\java.exe -jar backend\target\bili-agent-backend-0.0.1-SNAPSHOT.jar
```

---

## 9. 怎么判断启动成功

看到类似下面的日志，就说明启动成功了：

```text
Tomcat started on port 8080
Started BiliAgentApplication
```

项目默认端口是：

```text
8080
```

---

## 10. 启动后怎么做最小验证

按下面顺序验证主链路。

### 第一步：注册

`POST /api/auth/register`

请求体：

```json
{
  "username": "demo-user",
  "password": "Password123!",
  "nickname": "Demo User"
}
```

预期返回：

```json
"registered"
```

### 第二步：登录

`POST /api/auth/login`

请求体：

```json
{
  "username": "demo-user",
  "password": "Password123!"
}
```

预期返回：

```json
{
  "token": "..."
}
```

后面的接口都要带：

```text
Authorization: Bearer 你的token
```

### 第三步：绑定 B 站账号

`POST /api/bili/bind`

请求体：

```json
{
  "biliUid": "123456",
  "cookieSnapshot": "SESSDATA=demo-cookie"
}
```

预期返回里会有：

```text
userId
biliUid
bindStatus
```

### 第四步：同步视频

`POST /api/bili/sync`

这个接口不需要请求体。

预期会返回统计字段：

```text
historyCount
favoritesCount
watchLaterCount
uniqueVideoCount
sourceRecordCount
syncedAt
```

### 第五步：创建分析任务

`POST /api/analysis/tasks`

请求体：

```json
{
  "videoId": 101,
  "analysisType": "SUMMARY"
}
```

预期返回：

```json
"PENDING"
```

### 第六步：创建知识卡片

`POST /api/knowledge/cards`

请求体：

```json
{
  "videoId": 101,
  "analysisTaskId": 1,
  "analysisResultId": 1
}
```

预期返回：

```json
"knowledge-card-created"
```

### 第七步：问答

`POST /api/qa/ask`

请求体：

```json
{
  "question": "这条视频的核心观点是什么？"
}
```

预期返回里会有：

```text
answer
sourceRefs
```

---

## 11. 这轮已经验证通过的事实

当前项目已经实际验证过：

1. 后端可以启动
2. 全量测试通过：`28 tests, 0 failures`
3. 主链路接口能跑通：
   - 注册
   - 登录
   - 绑定
   - 同步
   - 创建分析任务
   - 创建知识卡片
   - 问答

---

## 12. 常见问题排查

### 问题 1：`java -version` 还是 1.8

说明你还没切到 `Java 21`。

重新执行：

```powershell
$env:JAVA_HOME="C:\tmp\oracle-jdk-21"
$env:Path="$env:JAVA_HOME\bin;$env:Path"
java -version
```

### 问题 2：MySQL 连不上

先检查：

```powershell
Test-NetConnection -ComputerName 127.0.0.1 -Port 3306
```

再检查：

1. MySQL 服务是不是启动了
2. `backend/.env.local` 里的用户名密码是不是对的
3. 数据库 `bili_agent` 是不是已经创建

### 问题 3：报 `Public Key Retrieval is not allowed`

说明 JDBC URL 不完整。

确认你的 `APP_DB_URL` 里包含：

```text
allowPublicKeyRetrieval=true
```

### 问题 4：Redis 连不上

先检查：

```powershell
Test-NetConnection -ComputerName 127.0.0.1 -Port 6379
```

如果不通，先启动 Redis 或 Memurai。

### 问题 5：启动成功但接口 401

一般是因为：

1. 你没先登录
2. 你没带 `Bearer Token`
3. token 复制错了

---

## 13. 不要提交这些文件

下面这些文件不要带进提交：

1. `backend/.env.local`
2. `backend/target/`
3. `notes/`
4. 其他本地截图、临时文件、安装包

---

## 14. 推荐接手顺序

如果你是第一次接手，建议按这个顺序：

1. 先看这份 `项目启动说明书.md`
2. 再看 `backend/README.md`
3. 跑 `backend\mvnw.cmd -f backend/pom.xml test`
4. 再跑启动命令
5. 最后按主链路接口验收

这样最不容易乱。

---

一句话科普：`Spring Boot` 在这个项目里主要负责把接口、配置、数据库连接和鉴权流程组织成一个能直接运行的后端服务。
